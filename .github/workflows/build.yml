name: CI

on:
  push:

jobs:
  build:
    name: build and test
    runs-on: "${{ matrix.os }}"
    steps:
      - uses: actions/checkout@v4
      - uses: cachix/install-nix-action@v24
      - run: nix build
